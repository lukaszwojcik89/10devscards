name: Master Branch CI/CD

on:
  push:
    branches: [master, main]
  workflow_dispatch:

env:
  NODE_VERSION: "18"

jobs:
  test:
    name: Run Tests
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Run linting
        run: npm run lint

      - name: Run unit tests with coverage
        run: npm run test:run -- --coverage

      - name: Upload coverage report
        uses: actions/upload-artifact@v4
        with:
          name: test-coverage
          path: coverage/
          retention-days: 30

  build:
    name: Build Application
    runs-on: ubuntu-latest
    needs: test
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: "npm"

      - name: Install dependencies
        run: npm ci

      - name: Build for production
        run: npm run build

      - name: Upload build artifacts
        uses: actions/upload-artifact@v4
        with:
          name: production-build
          path: dist/
          retention-days: 30

  quality-gate:
    name: Quality Gate
    runs-on: ubuntu-latest
    needs: [test, build]
    steps:
      - name: Download coverage report
        uses: actions/download-artifact@v4
        with:
          name: test-coverage
          path: coverage/

      - name: Quality gate check
        run: |
          echo "🔍 Sprawdzanie jakości kodu..."
          echo "✅ Testy jednostkowe: PASSED"
          echo "✅ Build produkcyjny: PASSED"
          echo "✅ Pokrycie testami dostępne w artifaktach"
          echo "🎉 Quality Gate: PASSED"

  notify-success:
    name: Notify Success
    runs-on: ubuntu-latest
    needs: [test, build, quality-gate]
    if: success()
    steps:
      - name: Success notification
        run: |
          echo "🎉 Deployment do master zakończony sukcesem!"
          echo "📊 Statystyki:"
          echo "   - Testy jednostkowe: ✅ Przeszły"
          echo "   - Build: ✅ Gotowy"
          echo "   - Quality Gate: ✅ Przeszedł"
